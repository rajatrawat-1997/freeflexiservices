import React from 'react'

const NoData = ({width, height}) => {
    return (
        <React.Fragment>
            <img src="../assets/images/no-data.png" width={width} height={height} />
        </React.Fragment>
    )
}

export default NoData
